# Challenge + Reward + Fail Flow

- Reward: when user crosses a cultivation milestone day.
- Challenge: special prompt at day 8 (can extend later).
- Fail: user taps the fail button and sees hardcore/safe copy.

Tone is controlled in src/constants/appConfig.js via CHALLENGE_TONE.
Copy theme for the app is controlled by COPY_THEME.
